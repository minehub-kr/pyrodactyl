// million-ignore
const Logo = ({ className, uniqueId }: { className?: string; uniqueId?: string } = {}) => {
    return (
        <div className={className}>
            Minehub Cloud
        </div>
    );
};

export default Logo;
